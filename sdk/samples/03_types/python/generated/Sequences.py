"""
Generated by hddsgen v1.0.9
DO NOT EDIT

Python target (dataclasses + typing + CDR2 serialization)
"""

from dataclasses import dataclass, field
from typing import List, Dict, Optional, Union as UnionType, Tuple
from enum import IntEnum
import struct
import hashlib

LongSeq = List[int]

StringSeq = List[str]

BoundedLongSeq = List[int]

@dataclass
class Sequences:
    """IDL: struct Sequences { LongSeq numbers; StringSeq names; BoundedLongSeq bounded_numbers; }"""
    numbers: LongSeq = field(default_factory=list)
    names: StringSeq = field(default_factory=list)
    bounded_numbers: BoundedLongSeq = field(default_factory=list)

    def encode_cdr2_le(self) -> bytes:
        """Encode to CDR2 little-endian format (FINAL extensibility)."""
        parts: List[bytes] = []
        offset = 0
        # sequence: align to 4, write length, then elements
        pad = (4 - (offset % 4)) % 4
        parts.append(b'\x00' * pad)
        offset += pad
        parts.append(struct.pack('<I', len(self.numbers)))
        offset += 4
        for _elem in self.numbers:
            pad = (4 - (offset % 4)) % 4
            parts.append(b'\x00' * pad)
            offset += pad
            parts.append(struct.pack('<i', _elem))
            offset += 4
        # sequence: align to 4, write length, then elements
        pad = (4 - (offset % 4)) % 4
        parts.append(b'\x00' * pad)
        offset += pad
        parts.append(struct.pack('<I', len(self.names)))
        offset += 4
        for _elem in self.names:
            pad = (4 - (offset % 4)) % 4
            parts.append(b'\x00' * pad)
            offset += pad
            _encoded = _elem.encode('utf-8') + b'\x00'
            parts.append(struct.pack('<I', len(_encoded)))
            offset += 4
            parts.append(_encoded)
            offset += len(_encoded)
        # sequence: align to 4, write length, then elements
        pad = (4 - (offset % 4)) % 4
        parts.append(b'\x00' * pad)
        offset += pad
        parts.append(struct.pack('<I', len(self.bounded_numbers)))
        offset += 4
        for _elem in self.bounded_numbers:
            pad = (4 - (offset % 4)) % 4
            parts.append(b'\x00' * pad)
            offset += pad
            parts.append(struct.pack('<i', _elem))
            offset += 4
        return b''.join(parts)

    @classmethod
    def decode_cdr2_le(cls, data: bytes) -> Tuple['Sequences', int]:
        """Decode from CDR2 little-endian format (FINAL extensibility). Returns (instance, bytes_read)."""
        offset = 0
        # sequence: align, read length, then elements
        offset += (4 - (offset % 4)) % 4
        _seq_len, = struct.unpack_from('<I', data, offset)
        offset += 4
        _numbers = []
        for _ in range(_seq_len):
            offset += (4 - (offset % 4)) % 4
            _elem, = struct.unpack_from('<i', data, offset)
            offset += 4
            _numbers.append(_elem)
        # sequence: align, read length, then elements
        offset += (4 - (offset % 4)) % 4
        _seq_len, = struct.unpack_from('<I', data, offset)
        offset += 4
        _names = []
        for _ in range(_seq_len):
            offset += (4 - (offset % 4)) % 4
            _slen, = struct.unpack_from('<I', data, offset)
            offset += 4
            _elem = data[offset:offset+_slen-1].decode('utf-8')
            offset += _slen
            _names.append(_elem)
        # sequence: align, read length, then elements
        offset += (4 - (offset % 4)) % 4
        _seq_len, = struct.unpack_from('<I', data, offset)
        offset += 4
        _bounded_numbers = []
        for _ in range(_seq_len):
            offset += (4 - (offset % 4)) % 4
            _elem, = struct.unpack_from('<i', data, offset)
            offset += 4
            _bounded_numbers.append(_elem)
        return cls(numbers=_numbers, names=_names, bounded_numbers=_bounded_numbers), offset

    @classmethod
    def type_name(cls) -> str:
        """Return the DDS type name."""
        return "Sequences"

    def compute_key(self) -> bytes:
        """Compute instance key hash from @key fields (FNV-1a, 16 bytes)."""
        # No @key fields - return zeroed hash
        return bytes(16)

    @classmethod
    def has_key(cls) -> bool:
        """Return True if this type has @key fields."""
        return False

