"""
Generated by hddsgen v1.0.9
DO NOT EDIT

Python target (dataclasses + typing + CDR2 serialization)
"""

from dataclasses import dataclass, field
from typing import List, Dict, Optional, Union as UnionType, Tuple
from enum import IntEnum
import struct
import hashlib

@dataclass
class Point:
    """IDL: struct Point { double x; double y; double z; }"""
    x: float = 0.0
    y: float = 0.0
    z: float = 0.0

    def encode_cdr2_le(self) -> bytes:
        """Encode to CDR2 little-endian format (FINAL extensibility)."""
        parts: List[bytes] = []
        offset = 0
        # align to 8 and pack x
        pad = (8 - (offset % 8)) % 8
        parts.append(b'\x00' * pad)
        offset += pad
        parts.append(struct.pack('<d', self.x))
        offset += 8
        # align to 8 and pack y
        pad = (8 - (offset % 8)) % 8
        parts.append(b'\x00' * pad)
        offset += pad
        parts.append(struct.pack('<d', self.y))
        offset += 8
        # align to 8 and pack z
        pad = (8 - (offset % 8)) % 8
        parts.append(b'\x00' * pad)
        offset += pad
        parts.append(struct.pack('<d', self.z))
        offset += 8
        return b''.join(parts)

    @classmethod
    def decode_cdr2_le(cls, data: bytes) -> Tuple['Point', int]:
        """Decode from CDR2 little-endian format (FINAL extensibility). Returns (instance, bytes_read)."""
        offset = 0
        # align and unpack x
        offset += (8 - (offset % 8)) % 8
        _x, = struct.unpack_from('<d', data, offset)
        offset += 8
        # align and unpack y
        offset += (8 - (offset % 8)) % 8
        _y, = struct.unpack_from('<d', data, offset)
        offset += 8
        # align and unpack z
        offset += (8 - (offset % 8)) % 8
        _z, = struct.unpack_from('<d', data, offset)
        offset += 8
        return cls(x=_x, y=_y, z=_z), offset

    @classmethod
    def type_name(cls) -> str:
        """Return the DDS type name."""
        return "Point"

    def compute_key(self) -> bytes:
        """Compute instance key hash from @key fields (FNV-1a, 16 bytes)."""
        # No @key fields - return zeroed hash
        return bytes(16)

    @classmethod
    def has_key(cls) -> bool:
        """Return True if this type has @key fields."""
        return False

@dataclass
class Pose:
    """IDL: struct Pose { Point position; Point orientation; }"""
    position: Point = field(default_factory=Point)
    orientation: Point = field(default_factory=Point)

    def encode_cdr2_le(self) -> bytes:
        """Encode to CDR2 little-endian format (FINAL extensibility)."""
        parts: List[bytes] = []
        offset = 0
        # nested struct Point
        _sub = self.position.encode_cdr2_le()
        parts.append(_sub)
        offset += len(_sub)
        # nested struct Point
        _sub = self.orientation.encode_cdr2_le()
        parts.append(_sub)
        offset += len(_sub)
        return b''.join(parts)

    @classmethod
    def decode_cdr2_le(cls, data: bytes) -> Tuple['Pose', int]:
        """Decode from CDR2 little-endian format (FINAL extensibility). Returns (instance, bytes_read)."""
        offset = 0
        # nested struct Point
        _position, _read = Point.decode_cdr2_le(data[offset:])
        offset += _read
        # nested struct Point
        _orientation, _read = Point.decode_cdr2_le(data[offset:])
        offset += _read
        return cls(position=_position, orientation=_orientation), offset

    @classmethod
    def type_name(cls) -> str:
        """Return the DDS type name."""
        return "Pose"

    def compute_key(self) -> bytes:
        """Compute instance key hash from @key fields (FNV-1a, 16 bytes)."""
        # No @key fields - return zeroed hash
        return bytes(16)

    @classmethod
    def has_key(cls) -> bool:
        """Return True if this type has @key fields."""
        return False

@dataclass
class Robot:
    """IDL: struct Robot { string name; Pose pose; sequence<Point> trajectory; }"""
    name: str = ""
    pose: Pose = field(default_factory=Pose)
    trajectory: List[Point] = field(default_factory=list)

    def encode_cdr2_le(self) -> bytes:
        """Encode to CDR2 little-endian format (FINAL extensibility)."""
        parts: List[bytes] = []
        offset = 0
        # string: align to 4, write length (with NUL), then bytes
        pad = (4 - (offset % 4)) % 4
        parts.append(b'\x00' * pad)
        offset += pad
        _bytes = self.name.encode('utf-8') + b'\x00'
        parts.append(struct.pack('<I', len(_bytes)))
        offset += 4
        parts.append(_bytes)
        offset += len(_bytes)
        # nested struct Pose
        _sub = self.pose.encode_cdr2_le()
        parts.append(_sub)
        offset += len(_sub)
        # sequence: align to 4, write length, then elements
        pad = (4 - (offset % 4)) % 4
        parts.append(b'\x00' * pad)
        offset += pad
        parts.append(struct.pack('<I', len(self.trajectory)))
        offset += 4
        for _elem in self.trajectory:
            _sub = _elem.encode_cdr2_le()
            parts.append(_sub)
            offset += len(_sub)
        return b''.join(parts)

    @classmethod
    def decode_cdr2_le(cls, data: bytes) -> Tuple['Robot', int]:
        """Decode from CDR2 little-endian format (FINAL extensibility). Returns (instance, bytes_read)."""
        offset = 0
        # string: align, read length, then bytes
        offset += (4 - (offset % 4)) % 4
        _len, = struct.unpack_from('<I', data, offset)
        offset += 4
        _name = data[offset:offset+_len-1].decode('utf-8')
        offset += _len
        # nested struct Pose
        _pose, _read = Pose.decode_cdr2_le(data[offset:])
        offset += _read
        # sequence: align, read length, then elements
        offset += (4 - (offset % 4)) % 4
        _seq_len, = struct.unpack_from('<I', data, offset)
        offset += 4
        _trajectory = []
        for _ in range(_seq_len):
            _elem, _read = Point.decode_cdr2_le(data[offset:])
            offset += _read
            _trajectory.append(_elem)
        return cls(name=_name, pose=_pose, trajectory=_trajectory), offset

    @classmethod
    def type_name(cls) -> str:
        """Return the DDS type name."""
        return "Robot"

    def compute_key(self) -> bytes:
        """Compute instance key hash from @key fields (FNV-1a, 16 bytes)."""
        # No @key fields - return zeroed hash
        return bytes(16)

    @classmethod
    def has_key(cls) -> bool:
        """Return True if this type has @key fields."""
        return False

