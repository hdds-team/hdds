"""
Generated by hddsgen v1.0.9
DO NOT EDIT

Python target (dataclasses + typing + CDR2 serialization)
"""

from dataclasses import dataclass, field
from typing import List, Dict, Optional, Union as UnionType, Tuple
from enum import IntEnum
import struct
import hashlib

class Permissions(int):
    """Bitmask Permissions - integer with named flag constants."""

    READ = 1 << 0
    WRITE = 1 << 1
    EXECUTE = 1 << 2
    DELETE = 1 << 3

    def __new__(cls, value: int = 0):
        return super().__new__(cls, value)

    def __or__(self, other: int) -> 'Permissions':
        return Permissions(int.__or__(self, other))

    def __and__(self, other: int) -> 'Permissions':
        return Permissions(int.__and__(self, other))

    def __xor__(self, other: int) -> 'Permissions':
        return Permissions(int.__xor__(self, other))

    def __invert__(self) -> 'Permissions':
        return Permissions(int.__invert__(self) & 0xFFFFFFFFFFFFFFFF)

    def encode_cdr2_le(self) -> bytes:
        """Encode to CDR2 little-endian format (8 bytes)."""
        return struct.pack('<Q', int(self) & 0xFFFFFFFFFFFFFFFF)

    @classmethod
    def decode_cdr2_le(cls, data: bytes) -> Tuple['Permissions', int]:
        """Decode from CDR2 little-endian format. Returns (instance, bytes_read)."""
        val, = struct.unpack_from('<Q', data, 0)
        return cls(val), 8

@dataclass
class StatusFlags:
    """Bitset StatusFlags - stores packed bit fields in a 64-bit integer."""
    bits: int = 0

    @property
    def priority(self) -> int:
        """Get priority (width=4, position=0)."""
        return (self.bits >> 0) & 15

    @priority.setter
    def priority(self, v: int) -> None:
        mask = 15 << 0
        self.bits = (self.bits & ~mask) | ((v & 15) << 0)

    @property
    def active(self) -> int:
        """Get active (width=1, position=4)."""
        return (self.bits >> 4) & 1

    @active.setter
    def active(self, v: int) -> None:
        mask = 1 << 4
        self.bits = (self.bits & ~mask) | ((v & 1) << 4)

    @property
    def error(self) -> int:
        """Get error (width=1, position=5)."""
        return (self.bits >> 5) & 1

    @error.setter
    def error(self, v: int) -> None:
        mask = 1 << 5
        self.bits = (self.bits & ~mask) | ((v & 1) << 5)

    @property
    def warning(self) -> int:
        """Get warning (width=1, position=6)."""
        return (self.bits >> 6) & 1

    @warning.setter
    def warning(self, v: int) -> None:
        mask = 1 << 6
        self.bits = (self.bits & ~mask) | ((v & 1) << 6)

    def encode_cdr2_le(self) -> bytes:
        """Encode to CDR2 little-endian format (8 bytes)."""
        return struct.pack('<Q', self.bits & 0xFFFFFFFFFFFFFFFF)

    @classmethod
    def decode_cdr2_le(cls, data: bytes) -> Tuple['StatusFlags', int]:
        """Decode from CDR2 little-endian format. Returns (instance, bytes_read)."""
        # Align to 8 bytes
        bits, = struct.unpack_from('<Q', data, 0)
        return cls(bits=bits), 8

@dataclass
class Bits:
    """IDL: struct Bits { Permissions perms; StatusFlags flags; }"""
    perms: Permissions = 0
    flags: StatusFlags = 0

    def encode_cdr2_le(self) -> bytes:
        """Encode to CDR2 little-endian format (FINAL extensibility)."""
        parts: List[bytes] = []
        offset = 0
        # unknown type Permissions for perms
        # unknown type StatusFlags for flags
        return b''.join(parts)

    @classmethod
    def decode_cdr2_le(cls, data: bytes) -> Tuple['Bits', int]:
        """Decode from CDR2 little-endian format (FINAL extensibility). Returns (instance, bytes_read)."""
        offset = 0
        _perms = None  # unknown type Permissions
        _flags = None  # unknown type StatusFlags
        return cls(perms=_perms, flags=_flags), offset

    @classmethod
    def type_name(cls) -> str:
        """Return the DDS type name."""
        return "Bits"

    def compute_key(self) -> bytes:
        """Compute instance key hash from @key fields (FNV-1a, 16 bytes)."""
        # No @key fields - return zeroed hash
        return bytes(16)

    @classmethod
    def has_key(cls) -> bool:
        """Return True if this type has @key fields."""
        return False

