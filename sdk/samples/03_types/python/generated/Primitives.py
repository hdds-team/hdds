"""
Generated by hddsgen v1.0.9
DO NOT EDIT

Python target (dataclasses + typing + CDR2 serialization)
"""

from dataclasses import dataclass, field
from typing import List, Dict, Optional, Union as UnionType, Tuple
from enum import IntEnum
import struct
import hashlib

@dataclass
class Primitives:
    """IDL: struct Primitives { boolean bool_val; octet octet_val; char char_val; short short_val; uint16_t ushort_val; long long_val; uint32_t ulong_val; long long llong_val; uint64_t ullong_val; float float_val; double double_val; }"""
    bool_val: bool = False
    octet_val: int = 0
    char_val: int = 0
    short_val: int = 0
    ushort_val: int = 0
    long_val: int = 0
    ulong_val: int = 0
    llong_val: int = 0
    ullong_val: int = 0
    float_val: float = 0.0
    double_val: float = 0.0

    def encode_cdr2_le(self) -> bytes:
        """Encode to CDR2 little-endian format (FINAL extensibility)."""
        parts: List[bytes] = []
        offset = 0
        # align to 1 and pack bool_val
        pad = (1 - (offset % 1)) % 1
        parts.append(b'\x00' * pad)
        offset += pad
        parts.append(struct.pack('B', self.bool_val))
        offset += 1
        # align to 1 and pack octet_val
        pad = (1 - (offset % 1)) % 1
        parts.append(b'\x00' * pad)
        offset += pad
        parts.append(struct.pack('B', self.octet_val))
        offset += 1
        # align to 1 and pack char_val
        pad = (1 - (offset % 1)) % 1
        parts.append(b'\x00' * pad)
        offset += pad
        parts.append(struct.pack('b', self.char_val))
        offset += 1
        # align to 2 and pack short_val
        pad = (2 - (offset % 2)) % 2
        parts.append(b'\x00' * pad)
        offset += pad
        parts.append(struct.pack('<h', self.short_val))
        offset += 2
        # align to 2 and pack ushort_val
        pad = (2 - (offset % 2)) % 2
        parts.append(b'\x00' * pad)
        offset += pad
        parts.append(struct.pack('<H', self.ushort_val))
        offset += 2
        # align to 4 and pack long_val
        pad = (4 - (offset % 4)) % 4
        parts.append(b'\x00' * pad)
        offset += pad
        parts.append(struct.pack('<i', self.long_val))
        offset += 4
        # align to 4 and pack ulong_val
        pad = (4 - (offset % 4)) % 4
        parts.append(b'\x00' * pad)
        offset += pad
        parts.append(struct.pack('<I', self.ulong_val))
        offset += 4
        # align to 8 and pack llong_val
        pad = (8 - (offset % 8)) % 8
        parts.append(b'\x00' * pad)
        offset += pad
        parts.append(struct.pack('<q', self.llong_val))
        offset += 8
        # align to 8 and pack ullong_val
        pad = (8 - (offset % 8)) % 8
        parts.append(b'\x00' * pad)
        offset += pad
        parts.append(struct.pack('<Q', self.ullong_val))
        offset += 8
        # align to 4 and pack float_val
        pad = (4 - (offset % 4)) % 4
        parts.append(b'\x00' * pad)
        offset += pad
        parts.append(struct.pack('<f', self.float_val))
        offset += 4
        # align to 8 and pack double_val
        pad = (8 - (offset % 8)) % 8
        parts.append(b'\x00' * pad)
        offset += pad
        parts.append(struct.pack('<d', self.double_val))
        offset += 8
        return b''.join(parts)

    @classmethod
    def decode_cdr2_le(cls, data: bytes) -> Tuple['Primitives', int]:
        """Decode from CDR2 little-endian format (FINAL extensibility). Returns (instance, bytes_read)."""
        offset = 0
        # align and unpack bool_val
        offset += (1 - (offset % 1)) % 1
        _bool_val, = struct.unpack_from('B', data, offset)
        offset += 1
        # align and unpack octet_val
        offset += (1 - (offset % 1)) % 1
        _octet_val, = struct.unpack_from('B', data, offset)
        offset += 1
        # align and unpack char_val
        offset += (1 - (offset % 1)) % 1
        _char_val, = struct.unpack_from('b', data, offset)
        offset += 1
        # align and unpack short_val
        offset += (2 - (offset % 2)) % 2
        _short_val, = struct.unpack_from('<h', data, offset)
        offset += 2
        # align and unpack ushort_val
        offset += (2 - (offset % 2)) % 2
        _ushort_val, = struct.unpack_from('<H', data, offset)
        offset += 2
        # align and unpack long_val
        offset += (4 - (offset % 4)) % 4
        _long_val, = struct.unpack_from('<i', data, offset)
        offset += 4
        # align and unpack ulong_val
        offset += (4 - (offset % 4)) % 4
        _ulong_val, = struct.unpack_from('<I', data, offset)
        offset += 4
        # align and unpack llong_val
        offset += (8 - (offset % 8)) % 8
        _llong_val, = struct.unpack_from('<q', data, offset)
        offset += 8
        # align and unpack ullong_val
        offset += (8 - (offset % 8)) % 8
        _ullong_val, = struct.unpack_from('<Q', data, offset)
        offset += 8
        # align and unpack float_val
        offset += (4 - (offset % 4)) % 4
        _float_val, = struct.unpack_from('<f', data, offset)
        offset += 4
        # align and unpack double_val
        offset += (8 - (offset % 8)) % 8
        _double_val, = struct.unpack_from('<d', data, offset)
        offset += 8
        return cls(bool_val=_bool_val, octet_val=_octet_val, char_val=_char_val, short_val=_short_val, ushort_val=_ushort_val, long_val=_long_val, ulong_val=_ulong_val, llong_val=_llong_val, ullong_val=_ullong_val, float_val=_float_val, double_val=_double_val), offset

    @classmethod
    def type_name(cls) -> str:
        """Return the DDS type name."""
        return "Primitives"

    def compute_key(self) -> bytes:
        """Compute instance key hash from @key fields (FNV-1a, 16 bytes)."""
        # No @key fields - return zeroed hash
        return bytes(16)

    @classmethod
    def has_key(cls) -> bool:
        """Return True if this type has @key fields."""
        return False

