"""
Generated by hddsgen v1.0.9
DO NOT EDIT

Python target (dataclasses + typing + CDR2 serialization)
"""

from dataclasses import dataclass, field
from typing import List, Dict, Optional, Union as UnionType, Tuple
from enum import IntEnum
import struct
import hashlib

@dataclass
class Arrays:
    """IDL: struct Arrays { sequence<long, 10> numbers; sequence<string, 5> names; sequence<sequence<float, 3>, 3> transform; }"""
    numbers: List[int] = field(default_factory=list)
    names: List[str] = field(default_factory=list)
    transform: List[List[float]] = field(default_factory=list)

    def encode_cdr2_le(self) -> bytes:
        """Encode to CDR2 little-endian format (FINAL extensibility)."""
        parts: List[bytes] = []
        offset = 0
        # sequence: align to 4, write length, then elements
        pad = (4 - (offset % 4)) % 4
        parts.append(b'\x00' * pad)
        offset += pad
        parts.append(struct.pack('<I', len(self.numbers)))
        offset += 4
        for _elem in self.numbers:
            pad = (4 - (offset % 4)) % 4
            parts.append(b'\x00' * pad)
            offset += pad
            parts.append(struct.pack('<i', _elem))
            offset += 4
        # sequence: align to 4, write length, then elements
        pad = (4 - (offset % 4)) % 4
        parts.append(b'\x00' * pad)
        offset += pad
        parts.append(struct.pack('<I', len(self.names)))
        offset += 4
        for _elem in self.names:
            pad = (4 - (offset % 4)) % 4
            parts.append(b'\x00' * pad)
            offset += pad
            _encoded = _elem.encode('utf-8') + b'\x00'
            parts.append(struct.pack('<I', len(_encoded)))
            offset += 4
            parts.append(_encoded)
            offset += len(_encoded)
        # sequence: align to 4, write length, then elements
        pad = (4 - (offset % 4)) % 4
        parts.append(b'\x00' * pad)
        offset += pad
        parts.append(struct.pack('<I', len(self.transform)))
        offset += 4
        for _elem in self.transform:
            pad = (4 - (offset % 4)) % 4
            parts.append(b'\x00' * pad)
            offset += pad
            parts.append(struct.pack('<I', len(_elem)))
            offset += 4
            for _inner_elem in _elem:
                pad = (4 - (offset % 4)) % 4
                parts.append(b'\x00' * pad)
                offset += pad
                parts.append(struct.pack('<f', _inner_elem))
                offset += 4
        return b''.join(parts)

    @classmethod
    def decode_cdr2_le(cls, data: bytes) -> Tuple['Arrays', int]:
        """Decode from CDR2 little-endian format (FINAL extensibility). Returns (instance, bytes_read)."""
        offset = 0
        # sequence: align, read length, then elements
        offset += (4 - (offset % 4)) % 4
        _seq_len, = struct.unpack_from('<I', data, offset)
        offset += 4
        _numbers = []
        for _ in range(_seq_len):
            offset += (4 - (offset % 4)) % 4
            _elem, = struct.unpack_from('<i', data, offset)
            offset += 4
            _numbers.append(_elem)
        # sequence: align, read length, then elements
        offset += (4 - (offset % 4)) % 4
        _seq_len, = struct.unpack_from('<I', data, offset)
        offset += 4
        _names = []
        for _ in range(_seq_len):
            offset += (4 - (offset % 4)) % 4
            _slen, = struct.unpack_from('<I', data, offset)
            offset += 4
            _elem = data[offset:offset+_slen-1].decode('utf-8')
            offset += _slen
            _names.append(_elem)
        # sequence: align, read length, then elements
        offset += (4 - (offset % 4)) % 4
        _seq_len, = struct.unpack_from('<I', data, offset)
        offset += 4
        _transform = []
        for _ in range(_seq_len):
            offset += (4 - (offset % 4)) % 4
            _inner_len, = struct.unpack_from('<I', data, offset)
            offset += 4
            _inner_list = []
            for _ in range(_inner_len):
                offset += (4 - (offset % 4)) % 4
                _elem, = struct.unpack_from('<f', data, offset)
                offset += 4
                _inner_list.append(_elem)
            _transform.append(_inner_list)
        return cls(numbers=_numbers, names=_names, transform=_transform), offset

    @classmethod
    def type_name(cls) -> str:
        """Return the DDS type name."""
        return "Arrays"

    def compute_key(self) -> bytes:
        """Compute instance key hash from @key fields (FNV-1a, 16 bytes)."""
        # No @key fields - return zeroed hash
        return bytes(16)

    @classmethod
    def has_key(cls) -> bool:
        """Return True if this type has @key fields."""
        return False

