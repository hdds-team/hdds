"""
Generated by hddsgen v1.0.9
DO NOT EDIT

Python target (dataclasses + typing + CDR2 serialization)
"""

from dataclasses import dataclass, field
from typing import List, Dict, Optional, Union as UnionType, Tuple
from enum import IntEnum
import struct
import hashlib

class Color(IntEnum):
    RED = 0
    GREEN = 1
    BLUE = 2

class Status(IntEnum):
    UNKNOWN = 0
    ACTIVE = 10
    INACTIVE = 20
    ERROR = 100

@dataclass
class Enums:
    """IDL: struct Enums { Color color; Status status; }"""
    color: Color = 0
    status: Status = 0

    def encode_cdr2_le(self) -> bytes:
        """Encode to CDR2 little-endian format (FINAL extensibility)."""
        parts: List[bytes] = []
        offset = 0
        # enum as int32
        pad = (4 - (offset % 4)) % 4
        parts.append(b'\x00' * pad)
        offset += pad
        parts.append(struct.pack('<i', int(self.color)))
        offset += 4
        # enum as int32
        pad = (4 - (offset % 4)) % 4
        parts.append(b'\x00' * pad)
        offset += pad
        parts.append(struct.pack('<i', int(self.status)))
        offset += 4
        return b''.join(parts)

    @classmethod
    def decode_cdr2_le(cls, data: bytes) -> Tuple['Enums', int]:
        """Decode from CDR2 little-endian format (FINAL extensibility). Returns (instance, bytes_read)."""
        offset = 0
        # enum as int32
        offset += (4 - (offset % 4)) % 4
        _val, = struct.unpack_from('<i', data, offset)
        _color = Color(_val)
        offset += 4
        # enum as int32
        offset += (4 - (offset % 4)) % 4
        _val, = struct.unpack_from('<i', data, offset)
        _status = Status(_val)
        offset += 4
        return cls(color=_color, status=_status), offset

    @classmethod
    def type_name(cls) -> str:
        """Return the DDS type name."""
        return "Enums"

    def compute_key(self) -> bytes:
        """Compute instance key hash from @key fields (FNV-1a, 16 bytes)."""
        # No @key fields - return zeroed hash
        return bytes(16)

    @classmethod
    def has_key(cls) -> bool:
        """Return True if this type has @key fields."""
        return False

