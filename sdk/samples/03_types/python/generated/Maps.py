"""
Generated by hddsgen v1.0.9
DO NOT EDIT

Python target (dataclasses + typing + CDR2 serialization)
"""

from dataclasses import dataclass, field
from typing import List, Dict, Optional, Union as UnionType, Tuple
from enum import IntEnum
import struct
import hashlib

StringLongMap = Dict[str, int]

LongStringMap = Dict[int, str]

@dataclass
class Maps:
    """IDL: struct Maps { StringLongMap scores; LongStringMap labels; }"""
    scores: StringLongMap = field(default_factory=dict)
    labels: LongStringMap = field(default_factory=dict)

    def encode_cdr2_le(self) -> bytes:
        """Encode to CDR2 little-endian format (FINAL extensibility)."""
        parts: List[bytes] = []
        offset = 0
        # map: align to 4, write length, then key-value pairs
        pad = (4 - (offset % 4)) % 4
        parts.append(b'\x00' * pad)
        offset += pad
        parts.append(struct.pack('<I', len(self.scores)))
        offset += 4
        for _key, _val in self.scores.items():
            pad = (4 - (offset % 4)) % 4
            parts.append(b'\x00' * pad)
            offset += pad
            _encoded = _key.encode('utf-8') + b'\x00'
            parts.append(struct.pack('<I', len(_encoded)))
            offset += 4
            parts.append(_encoded)
            offset += len(_encoded)
            pad = (4 - (offset % 4)) % 4
            parts.append(b'\x00' * pad)
            offset += pad
            parts.append(struct.pack('<i', _val))
            offset += 4
        # map: align to 4, write length, then key-value pairs
        pad = (4 - (offset % 4)) % 4
        parts.append(b'\x00' * pad)
        offset += pad
        parts.append(struct.pack('<I', len(self.labels)))
        offset += 4
        for _key, _val in self.labels.items():
            pad = (4 - (offset % 4)) % 4
            parts.append(b'\x00' * pad)
            offset += pad
            parts.append(struct.pack('<i', _key))
            offset += 4
            pad = (4 - (offset % 4)) % 4
            parts.append(b'\x00' * pad)
            offset += pad
            _encoded = _val.encode('utf-8') + b'\x00'
            parts.append(struct.pack('<I', len(_encoded)))
            offset += 4
            parts.append(_encoded)
            offset += len(_encoded)
        return b''.join(parts)

    @classmethod
    def decode_cdr2_le(cls, data: bytes) -> Tuple['Maps', int]:
        """Decode from CDR2 little-endian format (FINAL extensibility). Returns (instance, bytes_read)."""
        offset = 0
        # map: align, read length, then key-value pairs
        offset += (4 - (offset % 4)) % 4
        _map_len, = struct.unpack_from('<I', data, offset)
        offset += 4
        _scores = {}
        for _ in range(_map_len):
            offset += (4 - (offset % 4)) % 4
            _slen, = struct.unpack_from('<I', data, offset)
            offset += 4
            _k = data[offset:offset+_slen-1].decode('utf-8')
            offset += _slen
            offset += (4 - (offset % 4)) % 4
            _v, = struct.unpack_from('<i', data, offset)
            offset += 4
            _scores[_k] = _v
        # map: align, read length, then key-value pairs
        offset += (4 - (offset % 4)) % 4
        _map_len, = struct.unpack_from('<I', data, offset)
        offset += 4
        _labels = {}
        for _ in range(_map_len):
            offset += (4 - (offset % 4)) % 4
            _k, = struct.unpack_from('<i', data, offset)
            offset += 4
            offset += (4 - (offset % 4)) % 4
            _slen, = struct.unpack_from('<I', data, offset)
            offset += 4
            _v = data[offset:offset+_slen-1].decode('utf-8')
            offset += _slen
            _labels[_k] = _v
        return cls(scores=_scores, labels=_labels), offset

    @classmethod
    def type_name(cls) -> str:
        """Return the DDS type name."""
        return "Maps"

    def compute_key(self) -> bytes:
        """Compute instance key hash from @key fields (FNV-1a, 16 bytes)."""
        # No @key fields - return zeroed hash
        return bytes(16)

    @classmethod
    def has_key(cls) -> bool:
        """Return True if this type has @key fields."""
        return False

