// Generated by hddsgen v1.0.9
// DO NOT EDIT

#pragma once

#include <cstddef>
#include <cstdint>
#include <cstring>
#include <cstdlib>
#include <new>
#include <utility>

// CDR2 serialization helpers (with include guard to allow multiple IDL files)
#ifndef HDDS_CDR2_HELPERS_DEFINED
#define HDDS_CDR2_HELPERS_DEFINED
namespace cdr2 {

inline std::size_t align_offset(std::size_t offset, std::size_t alignment) noexcept {
    return (offset + alignment - 1) & ~(alignment - 1);
}

inline bool can_write(std::size_t len, std::size_t offset, std::size_t bytes) noexcept {
    return offset + bytes <= len;
}

inline bool can_read(std::size_t len, std::size_t offset, std::size_t bytes) noexcept {
    return offset + bytes <= len;
}

template<typename T>
inline void write_le(std::uint8_t* dst, std::size_t& offset, T value) noexcept {
    std::memcpy(dst + offset, &value, sizeof(T));
    offset += sizeof(T);
}

template<typename T>
inline T read_le(const std::uint8_t* src, std::size_t& offset) noexcept {
    T value;
    std::memcpy(&value, src + offset, sizeof(T));
    offset += sizeof(T);
    return value;
}

}  // namespace cdr2
#endif // HDDS_CDR2_HELPERS_DEFINED

namespace hdds_samples {

    struct Primitives {
        bool bool_val = false;
        uint8_t octet_val = 0;
        char char_val = 0;
        int16_t short_val = 0;
        uint16_t ushort_val = 0;
        int32_t long_val = 0;
        uint32_t ulong_val = 0;
        int64_t llong_val = 0;
        uint64_t ullong_val = 0;
        float float_val = 0.0f;
        double double_val = 0.0;

        Primitives() = default;
        Primitives(bool bool_val, uint8_t octet_val, char char_val, int16_t short_val, uint16_t ushort_val, int32_t long_val, uint32_t ulong_val, int64_t llong_val, uint64_t ullong_val, float float_val, double double_val)
            : bool_val(std::move(bool_val)), octet_val(std::move(octet_val)), char_val(std::move(char_val)), short_val(std::move(short_val)), ushort_val(std::move(ushort_val)), long_val(std::move(long_val)), ulong_val(std::move(ulong_val)), llong_val(std::move(llong_val)), ullong_val(std::move(ullong_val)), float_val(std::move(float_val)), double_val(std::move(double_val)) {}

        /// Encode this struct to CDR2 little-endian format
        /// Returns the number of bytes written, or -1 on error
        [[nodiscard]] int encode_cdr2_le(std::uint8_t* dst, std::size_t len) const noexcept {
            std::size_t offset = 0;
            offset = cdr2::align_offset(offset, 1);
            if (!cdr2::can_write(len, offset, 1)) return -1;
            std::memcpy(dst + offset, &(this->bool_val), 1);
            offset += 1;
            offset = cdr2::align_offset(offset, 1);
            if (!cdr2::can_write(len, offset, 1)) return -1;
            std::memcpy(dst + offset, &(this->octet_val), 1);
            offset += 1;
            offset = cdr2::align_offset(offset, 1);
            if (!cdr2::can_write(len, offset, 1)) return -1;
            std::memcpy(dst + offset, &(this->char_val), 1);
            offset += 1;
            offset = cdr2::align_offset(offset, 2);
            if (!cdr2::can_write(len, offset, 2)) return -1;
            std::memcpy(dst + offset, &(this->short_val), 2);
            offset += 2;
            offset = cdr2::align_offset(offset, 2);
            if (!cdr2::can_write(len, offset, 2)) return -1;
            std::memcpy(dst + offset, &(this->ushort_val), 2);
            offset += 2;
            offset = cdr2::align_offset(offset, 4);
            if (!cdr2::can_write(len, offset, 4)) return -1;
            std::memcpy(dst + offset, &(this->long_val), 4);
            offset += 4;
            offset = cdr2::align_offset(offset, 4);
            if (!cdr2::can_write(len, offset, 4)) return -1;
            std::memcpy(dst + offset, &(this->ulong_val), 4);
            offset += 4;
            offset = cdr2::align_offset(offset, 8);
            if (!cdr2::can_write(len, offset, 8)) return -1;
            std::memcpy(dst + offset, &(this->llong_val), 8);
            offset += 8;
            offset = cdr2::align_offset(offset, 8);
            if (!cdr2::can_write(len, offset, 8)) return -1;
            std::memcpy(dst + offset, &(this->ullong_val), 8);
            offset += 8;
            offset = cdr2::align_offset(offset, 4);
            if (!cdr2::can_write(len, offset, 4)) return -1;
            std::memcpy(dst + offset, &(this->float_val), 4);
            offset += 4;
            offset = cdr2::align_offset(offset, 8);
            if (!cdr2::can_write(len, offset, 8)) return -1;
            std::memcpy(dst + offset, &(this->double_val), 8);
            offset += 8;
            return static_cast<int>(offset);
        }

        /// Decode this struct from CDR2 little-endian format
        /// Returns the number of bytes read, or -1 on error
        [[nodiscard]] int decode_cdr2_le(const std::uint8_t* src, std::size_t len) noexcept {
            std::size_t offset = 0;
            offset = cdr2::align_offset(offset, 1);
            if (!cdr2::can_read(len, offset, 1)) return -1;
            std::memcpy(&(this->bool_val), src + offset, 1);
            offset += 1;
            offset = cdr2::align_offset(offset, 1);
            if (!cdr2::can_read(len, offset, 1)) return -1;
            std::memcpy(&(this->octet_val), src + offset, 1);
            offset += 1;
            offset = cdr2::align_offset(offset, 1);
            if (!cdr2::can_read(len, offset, 1)) return -1;
            std::memcpy(&(this->char_val), src + offset, 1);
            offset += 1;
            offset = cdr2::align_offset(offset, 2);
            if (!cdr2::can_read(len, offset, 2)) return -1;
            std::memcpy(&(this->short_val), src + offset, 2);
            offset += 2;
            offset = cdr2::align_offset(offset, 2);
            if (!cdr2::can_read(len, offset, 2)) return -1;
            std::memcpy(&(this->ushort_val), src + offset, 2);
            offset += 2;
            offset = cdr2::align_offset(offset, 4);
            if (!cdr2::can_read(len, offset, 4)) return -1;
            std::memcpy(&(this->long_val), src + offset, 4);
            offset += 4;
            offset = cdr2::align_offset(offset, 4);
            if (!cdr2::can_read(len, offset, 4)) return -1;
            std::memcpy(&(this->ulong_val), src + offset, 4);
            offset += 4;
            offset = cdr2::align_offset(offset, 8);
            if (!cdr2::can_read(len, offset, 8)) return -1;
            std::memcpy(&(this->llong_val), src + offset, 8);
            offset += 8;
            offset = cdr2::align_offset(offset, 8);
            if (!cdr2::can_read(len, offset, 8)) return -1;
            std::memcpy(&(this->ullong_val), src + offset, 8);
            offset += 8;
            offset = cdr2::align_offset(offset, 4);
            if (!cdr2::can_read(len, offset, 4)) return -1;
            std::memcpy(&(this->float_val), src + offset, 4);
            offset += 4;
            offset = cdr2::align_offset(offset, 8);
            if (!cdr2::can_read(len, offset, 8)) return -1;
            std::memcpy(&(this->double_val), src + offset, 8);
            offset += 8;
            return static_cast<int>(offset);
        }
    };

// PubSubType for Primitives
// Generated by hddsgen

/// TypeSupport class for Primitives
/// Provides DDS-compatible serialization/deserialization interface
class PrimitivesPubSubType {
public:
    using type = Primitives;

    static constexpr const char* type_name() noexcept { return "Primitives"; }

    /// Serialize data to CDR2 little-endian format
    /// @param data Pointer to the Primitives to serialize
    /// @param buffer Output buffer
    /// @param buffer_size Size of output buffer
    /// @return Number of bytes written, or -1 on error
    static int serialize(const void* data, std::uint8_t* buffer, std::size_t buffer_size) noexcept {
        if (!data || !buffer) return -1;
        const auto* typed = static_cast<const Primitives*>(data);
        return typed->encode_cdr2_le(buffer, buffer_size);
    }

    /// Deserialize data from CDR2 little-endian format
    /// @param buffer Input buffer containing CDR2 data
    /// @param buffer_size Size of input buffer
    /// @param data Pointer to Primitives to fill
    /// @return Number of bytes read, or -1 on error
    static int deserialize(const std::uint8_t* buffer, std::size_t buffer_size, void* data) noexcept {
        if (!buffer || !data) return -1;
        auto* typed = static_cast<Primitives*>(data);
        return typed->decode_cdr2_le(buffer, buffer_size);
    }

    /// Calculate serialized size of data
    /// @param data Pointer to the Primitives
    /// @return Serialized size in bytes, or 0 on error
    static std::size_t calculate_serialized_size(const void* data) noexcept {
        if (!data) return 0;
        const auto* typed = static_cast<const Primitives*>(data);
        // Fast path: stack buffer for typical types
        std::uint8_t stack_buf[16384];
        int size = typed->encode_cdr2_le(stack_buf, sizeof(stack_buf));
        if (size > 0) return static_cast<std::size_t>(size);
        // Heap fallback for large types (up to 16 MB)
        for (std::size_t cap = 65536; cap <= 16777216; cap *= 4) {
            auto* p = static_cast<std::uint8_t*>(std::malloc(cap));
            if (!p) return 0;
            size = typed->encode_cdr2_le(p, cap);
            std::free(p);
            if (size > 0) return static_cast<std::size_t>(size);
        }
        return 0;
    }

    /// Create a new instance of Primitives
    /// @return Pointer to newly allocated Primitives, or nullptr on failure
    static void* create_data() noexcept {
        return new (std::nothrow) Primitives();
    }

    /// Delete an instance of Primitives
    /// @param data Pointer to Primitives to delete
    static void delete_data(void* data) noexcept {
        delete static_cast<Primitives*>(data);
    }

    /// Compute instance key (no @key fields defined)
    /// @return false (no key fields)
    static bool compute_key(const void* /*data*/, std::uint8_t key_buffer[16]) noexcept {
        std::memset(key_buffer, 0, 16);
        return false;
    }

    /// Check if this type has @key fields
    static constexpr bool has_key() noexcept { return false; }

    /// Check if type has bounded size
    static constexpr bool is_bounded() noexcept { return true; }

    /// Check if type is plain (no pointers/dynamic allocation)
    static constexpr bool is_plain() noexcept { return true; }
};

}  // namespace hdds_samples

