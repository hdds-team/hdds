[package]
name = "hdds-schema-registry"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0 OR MIT"
description = "Schema Registry for HDDS DDS types"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
log = "0.4"

[dev-dependencies]
tempfile = "3.0"
